<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.CameraMega" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.CameraMega.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="splashScreenIconSize">240dp</item>
        <item name="postSplashScreenTheme">@style/Theme.CameraMega</item>
    </style>

    <style name="Theme.CameraMega.Transparent" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
    </style>
</resources>
